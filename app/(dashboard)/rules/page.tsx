'use client';

import DashboardPage from '@/app/(dashboard)/dashboard/page';

export default function RulesPage() {
    // Both Dashboard and Rules show the rules table as per request
    return <DashboardPage />;
}
